model Users {
  id         String   @id @default(uuid())
  name       String   @unique
  email      String
  password   String
  language   String
  phone      String
  created_at DateTime @default(now())
  updated_at DateTime @default(now())
}
